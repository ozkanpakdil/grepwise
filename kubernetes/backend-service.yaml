apiVersion: v1
kind: Service
metadata:
  name: grepwise-backend
  namespace: grepwise
  labels:
    app: grepwise
    component: backend
spec:
  selector:
    app: grepwise
    component: backend
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP