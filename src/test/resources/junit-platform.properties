# Enable parallel execution
junit.jupiter.execution.parallel.enabled=true

# Use dynamic parallelism (adjusts based on available processors)
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.mode.classes.default=concurrent

# Configure the parallelism
junit.jupiter.execution.parallel.config.strategy=dynamic
junit.jupiter.execution.parallel.config.dynamic.factor=1.0

# Optional: Set a fixed parallelism if you prefer
# junit.jupiter.execution.parallel.config.strategy=fixed
# junit.jupiter.execution.parallel.config.fixed.parallelism=4

# Optional: Set a custom executor for parallel tests
# junit.jupiter.execution.parallel.config.custom.class=com.example.CustomExecutor